# Exercício de SQL: Consultando Cidades Americanas

## 📋 Descrição do Exercício

**Objetivo**: Consultar todas as colunas de todas as cidades americanas na tabela CITY com populações maiores que 100.000. O CountryCode para América é USA.

### Estrutura da Tabela CITY

| Field       | Type         |
| ----------- | ------------ |
| ID          | NUMBER       |
| NAME        | VARCHAR2(17) |
| COUNTRYCODE | VARCHAR2(3)  |
| DISTRICT    | VARCHAR2(20) |
| POPULATION  | NUMBER       |

---

## 💡 Solução

```sql
SELECT *
FROM CITY
WHERE COUNTRYCODE = 'USA'
  AND POPULATION > 100000;
```

---

## 📚 Conceitos Fundamentais para Iniciantes

### 1. O que é SELECT?

O comando `SELECT` é usado para consultar (recuperar) dados de uma tabela no banco de dados.

**Sintaxe básica:**

```sql
SELECT coluna1, coluna2, ...
FROM nome_da_tabela;
```

**Exemplos:**

```sql
-- Selecionar colunas específicas
SELECT NAME, POPULATION FROM CITY;

-- Selecionar todas as colunas (usando *)
SELECT * FROM CITY;
```

**💡 Dica:** O asterisco (\*) significa "todas as colunas".

---

### 2. O que é FROM?

A cláusula `FROM` especifica de qual tabela você quer buscar os dados.

**Exemplo:**

```sql
SELECT * FROM CITY;
-- Isso busca dados da tabela chamada CITY
```

---

### 3. O que é WHERE?

A cláusula `WHERE` filtra os resultados com base em condições específicas. Só retorna as linhas que atendem às condições.

**Sintaxe:**

```sql
SELECT * FROM tabela
WHERE condição;
```

**Exemplos:**

```sql
-- Buscar cidades com população maior que 500000
SELECT * FROM CITY
WHERE POPULATION > 500000;

-- Buscar uma cidade específica por nome
SELECT * FROM CITY
WHERE NAME = 'New York';
```

---

### 4. Operadores de Comparação

| Operador | Significado    | Exemplo                      |
| -------- | -------------- | ---------------------------- |
| =        | Igual a        | `WHERE NAME = 'Boston'`      |
| >        | Maior que      | `WHERE POPULATION > 100000`  |
| <        | Menor que      | `WHERE POPULATION < 50000`   |
| >=       | Maior ou igual | `WHERE POPULATION >= 100000` |
| <=       | Menor ou igual | `WHERE POPULATION <= 500000` |
| <> ou != | Diferente de   | `WHERE COUNTRYCODE <> 'USA'` |

---

### 5. Operadores Lógicos: AND e OR

#### AND (E)

Retorna resultados que atendem **TODAS** as condições.

```sql
-- Ambas condições devem ser verdadeiras
SELECT * FROM CITY
WHERE COUNTRYCODE = 'USA'
  AND POPULATION > 100000;
```

**Explicação:** Retorna apenas cidades que são dos EUA **E** têm população maior que 100.000.

#### OR (OU)

Retorna resultados que atendem **PELO MENOS UMA** das condições.

```sql
-- Pelo menos uma condição deve ser verdadeira
SELECT * FROM CITY
WHERE COUNTRYCODE = 'USA'
   OR COUNTRYCODE = 'CAN';
```

**Explicação:** Retorna cidades que são dos EUA **OU** do Canadá.

---

### 6. Combinando Múltiplas Condições

Você pode combinar AND e OR, mas use parênteses para clareza:

```sql
SELECT * FROM CITY
WHERE (COUNTRYCODE = 'USA' OR COUNTRYCODE = 'CAN')
  AND POPULATION > 100000;
```

**Explicação:** Retorna cidades dos EUA ou Canadá que tenham população maior que 100.000.

---

## 🔍 Análise Detalhada da Solução

Vamos analisar a solução passo a passo:

```sql
SELECT *
FROM CITY
WHERE COUNTRYCODE = 'USA'
  AND POPULATION > 100000;
```

### Passo 1: SELECT \*

- `SELECT *` significa "selecione todas as colunas"
- Retornará: ID, NAME, COUNTRYCODE, DISTRICT, POPULATION

### Passo 2: FROM CITY

- Indica que estamos buscando dados da tabela CITY

### Passo 3: WHERE COUNTRYCODE = 'USA'

- Filtra apenas as cidades onde o código do país é 'USA'
- Note as aspas simples para valores de texto

### Passo 4: AND POPULATION > 100000

- Adiciona uma segunda condição: população maior que 100.000
- Ambas condições devem ser verdadeiras (por causa do AND)

---

## 🎯 Exercícios Práticos para Praticar

Tente resolver estes exercícios relacionados:

### Exercício 1 (Fácil)

Selecione todas as cidades dos EUA (sem filtro de população).

```sql
-- Sua resposta aqui
```

<details>
<summary>Ver Solução</summary>

```sql
SELECT * FROM CITY
WHERE COUNTRYCODE = 'USA';
```

</details>

### Exercício 2 (Fácil)

Selecione apenas o nome e a população de todas as cidades.

```sql
-- Sua resposta aqui
```

<details>
<summary>Ver Solução</summary>

```sql
SELECT NAME, POPULATION FROM CITY;
```

</details>

### Exercício 3 (Médio)

Selecione cidades dos EUA com população entre 100.000 e 500.000.

```sql
-- Sua resposta aqui
```

<details>
<summary>Ver Solução</summary>

```sql
SELECT * FROM CITY
WHERE COUNTRYCODE = 'USA'
  AND POPULATION > 100000
  AND POPULATION < 500000;

-- Ou usando BETWEEN:
SELECT * FROM CITY
WHERE COUNTRYCODE = 'USA'
  AND POPULATION BETWEEN 100001 AND 499999;
```

</details>

### Exercício 4 (Médio)

Selecione cidades dos EUA OU do Canadá (CAN) com população maior que 200.000.

```sql
-- Sua resposta aqui
```

<details>
<summary>Ver Solução</summary>

```sql
SELECT * FROM CITY
WHERE (COUNTRYCODE = 'USA' OR COUNTRYCODE = 'CAN')
  AND POPULATION > 200000;
```

</details>

---

## 📖 Comandos SQL Básicos - Resumo

| Comando | Propósito                   | Exemplo                     |
| ------- | --------------------------- | --------------------------- |
| SELECT  | Selecionar colunas          | `SELECT NAME, POPULATION`   |
| \*      | Todas as colunas            | `SELECT *`                  |
| FROM    | Especificar tabela          | `FROM CITY`                 |
| WHERE   | Filtrar resultados          | `WHERE POPULATION > 100000` |
| AND     | Ambas condições verdadeiras | `WHERE A AND B`             |
| OR      | Pelo menos uma verdadeira   | `WHERE A OR B`              |

---

## 💡 Dicas Importantes

1. **Aspas Simples para Texto**: Use aspas simples para valores de texto

   ```sql
   WHERE NAME = 'Boston'  ✅
   WHERE NAME = "Boston"  ❌ (funciona em alguns bancos, mas não é padrão)
   ```

2. **Sem Aspas para Números**: Não use aspas para números

   ```sql
   WHERE POPULATION > 100000  ✅
   WHERE POPULATION > '100000'  ❌
   ```

3. **SQL não é Case-Sensitive** (geralmente):

   ```sql
   SELECT * FROM CITY;
   select * from city;
   -- Ambos funcionam, mas MAIÚSCULAS são convenção para palavras-chave
   ```

4. **Nomes de Colunas e Tabelas**: Podem ser case-sensitive dependendo do banco
   ```sql
   -- Seguro usar o mesmo case da definição da tabela
   WHERE COUNTRYCODE = 'USA'
   ```

---

## 🎓 Próximos Passos

Depois de dominar estes conceitos básicos, você pode aprender:

- **ORDER BY**: Ordenar resultados
- **LIMIT**: Limitar número de resultados
- **LIKE**: Buscar padrões em texto
- **IN**: Verificar múltiplos valores
- **JOINS**: Combinar dados de múltiplas tabelas
- **GROUP BY**: Agrupar e agregar dados
- **Funções de Agregação**: COUNT, SUM, AVG, MAX, MIN

---

## 📚 Recursos Adicionais

- [W3Schools SQL Tutorial](https://www.w3schools.com/sql/)
- [MySQL Official Documentation](https://dev.mysql.com/doc/)
- [SQLBolt - Interactive Tutorials](https://sqlbolt.com/)

---

## ✅ Checklist de Aprendizado

- [ ] Entendo o que faz SELECT
- [ ] Entendo o que faz FROM
- [ ] Entendo o que faz WHERE
- [ ] Sei usar operadores de comparação (>, <, =)
- [ ] Sei a diferença entre AND e OR
- [ ] Consigo escrever consultas com múltiplas condições
- [ ] Sei quando usar aspas simples
- [ ] Resolvi todos os exercícios práticos

---

**Boa sorte nos seus estudos de SQL! 🚀**
