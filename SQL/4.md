# Employee Salaries - HackerRank Solution

## 📋 Descrição do Problema

Escrever uma consulta SQL que retorne uma lista de nomes de funcionários que:

- Ganham **mais de $2000 por mês**
- Trabalham na empresa há **menos de 10 meses**
- Resultados devem ser ordenados por `employee_id` em ordem crescente

## 🗃️ Estrutura da Tabela

**Employee**
| Coluna | Tipo | Descrição |
|--------|------|-----------|
| employee_id | Integer | ID único do funcionário |
| name | String | Nome do funcionário |
| months | Integer | Número de meses trabalhados |
| salary | Integer | Salário mensal |

## ✅ Solução

### Para MySQL

```sql
SELECT name
FROM Employee
WHERE salary > 2000
  AND months < 10
ORDER BY employee_id ASC;
```

### Para PostgreSQL

```sql
SELECT name
FROM Employee
WHERE salary > 2000
  AND months < 10
ORDER BY employee_id ASC;
```

> **Nota:** A solução é idêntica para MySQL e PostgreSQL neste caso específico, pois utilizamos apenas comandos SQL padrão.

## 📚 Explicação dos Comandos (Para Iniciantes)

### 1. **SELECT name**

```sql
SELECT name
```

- `SELECT` é o comando que define **quais colunas queremos recuperar** do banco de dados
- `name` especifica que queremos apenas a coluna com o nome dos funcionários
- Poderíamos selecionar múltiplas colunas: `SELECT name, salary, months`

### 2. **FROM Employee**

```sql
FROM Employee
```

- `FROM` indica **de qual tabela** vamos buscar os dados
- `Employee` é o nome da tabela que contém as informações dos funcionários

### 3. **WHERE salary > 2000 AND months < 10**

```sql
WHERE salary > 2000
  AND months < 10
```

- `WHERE` é usado para **filtrar** os resultados baseado em condições
- `salary > 2000` filtra apenas funcionários com salário maior que 2000
- `AND` é um operador lógico que significa que **ambas** as condições devem ser verdadeiras
- `months < 10` filtra apenas funcionários com menos de 10 meses de empresa

**Operadores de comparação comuns:**

- `>` maior que
- `<` menor que
- `>=` maior ou igual a
- `<=` menor ou igual a
- `=` igual a
- `!=` ou `<>` diferente de

### 4. **ORDER BY employee_id ASC**

```sql
ORDER BY employee_id ASC
```

- `ORDER BY` **ordena** os resultados
- `employee_id` é a coluna usada como critério de ordenação
- `ASC` significa ordem **ascendente** (crescente: 1, 2, 3...)
- `DESC` seria ordem **descendente** (decrescente: 3, 2, 1...)

## 🎯 Exemplo Prático

**Dados de entrada:**

| employee_id | name     | months | salary |
| ----------- | -------- | ------ | ------ |
| 12228       | Rose     | 15     | 1968   |
| 33645       | Angela   | 1      | 3443   |
| 45692       | Frank    | 17     | 1608   |
| 56118       | Patrick  | 7      | 1345   |
| 59725       | Lisa     | 11     | 2330   |
| 74197       | Kimberly | 16     | 4372   |
| 78454       | Bonnie   | 8      | 1771   |
| 83565       | Michael  | 6      | 2017   |
| 98607       | Todd     | 5      | 3396   |
| 99989       | Joe      | 9      | 3573   |

**Resultado esperado:**

```
Angela
Michael
Todd
Joe
```

**Por quê?**

- Angela: salary = 3443 (> 2000) ✓ e months = 1 (< 10) ✓
- Michael: salary = 2017 (> 2000) ✓ e months = 6 (< 10) ✓
- Todd: salary = 3396 (> 2000) ✓ e months = 5 (< 10) ✓
- Joe: salary = 3573 (> 2000) ✓ e months = 9 (< 10) ✓

Ordenados por employee_id: 33645 < 83565 < 98607 < 99989

## 🔍 Conceitos Importantes

### Cláusula WHERE

A cláusula WHERE funciona como um **filtro**. Imagine que você tem uma lista de 100 funcionários e quer ver apenas aqueles que atendem certos critérios. O WHERE faz exatamente isso.

### Operador AND

Quando usamos `AND`, **todas** as condições devem ser verdadeiras simultaneamente:

```sql
-- Ambas as condições devem ser verdadeiras
WHERE salary > 2000 AND months < 10
```

Se usássemos `OR`, **pelo menos uma** das condições deveria ser verdadeira:

```sql
-- Pelo menos uma condição deve ser verdadeira
WHERE salary > 2000 OR months < 10
```

### ORDER BY

Ordenação é como organizar uma lista em ordem alfabética ou numérica:

- `ASC` (padrão): A → Z ou 1 → 10
- `DESC`: Z → A ou 10 → 1

## 🛠️ Como Testar

### No MySQL:

```sql
-- Criar a tabela
CREATE TABLE Employee (
    employee_id INT PRIMARY KEY,
    name VARCHAR(100),
    months INT,
    salary INT
);

-- Inserir dados de exemplo
INSERT INTO Employee VALUES
(12228, 'Rose', 15, 1968),
(33645, 'Angela', 1, 3443),
(45692, 'Frank', 17, 1608),
(56118, 'Patrick', 7, 1345),
(59725, 'Lisa', 11, 2330),
(74197, 'Kimberly', 16, 4372),
(78454, 'Bonnie', 8, 1771),
(83565, 'Michael', 6, 2017),
(98607, 'Todd', 5, 3396),
(99989, 'Joe', 9, 3573);

-- Executar a consulta
SELECT name
FROM Employee
WHERE salary > 2000
  AND months < 10
ORDER BY employee_id ASC;
```

### No PostgreSQL:

```sql
-- O código é idêntico ao MySQL
CREATE TABLE Employee (
    employee_id INT PRIMARY KEY,
    name VARCHAR(100),
    months INT,
    salary INT
);

INSERT INTO Employee VALUES
(12228, 'Rose', 15, 1968),
(33645, 'Angela', 1, 3443),
(45692, 'Frank', 17, 1608),
(56118, 'Patrick', 7, 1345),
(59725, 'Lisa', 11, 2330),
(74197, 'Kimberly', 16, 4372),
(78454, 'Bonnie', 8, 1771),
(83565, 'Michael', 6, 2017),
(98607, 'Todd', 5, 3396),
(99989, 'Joe', 9, 3573);

SELECT name
FROM Employee
WHERE salary > 2000
  AND months < 10
ORDER BY employee_id ASC;
```

## 💡 Dicas para Iniciantes

1. **SQL não é case-sensitive** (na maioria dos casos): `SELECT` = `select` = `Select`
2. **Use indentação** para deixar seu código mais legível
3. **Termine comandos com ponto e vírgula** (`;`)
4. **Teste suas consultas** em pequenos conjuntos de dados primeiro
5. **WHERE sempre vem antes de ORDER BY** na estrutura do SQL

## 🎓 Próximos Passos

Após dominar este exercício, você pode praticar:

- Usar `BETWEEN` para intervalos: `WHERE salary BETWEEN 2000 AND 5000`
- Usar `IN` para múltiplos valores: `WHERE months IN (1, 2, 3)`
- Combinar múltiplas condições com parênteses: `WHERE (salary > 2000 OR months < 5) AND name LIKE 'A%'`
- Funções de agregação: `COUNT()`, `SUM()`, `AVG()`, `MAX()`, `MIN()`

## 📖 Referências

- [MySQL Documentation](https://dev.mysql.com/doc/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [W3Schools SQL Tutorial](https://www.w3schools.com/sql/)
- [HackerRank SQL Practice](https://www.hackerrank.com/domains/sql)

---

**Dificuldade:** Fácil 🟢  
**Tópicos:** SELECT, WHERE, ORDER BY, Operadores Lógicos  
**Tempo estimado:** 5-10 minutos
